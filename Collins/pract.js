var ame = `Collins`;

console.log(ame);

function fund(p, r) {
    sum = p + r;

    return sum;
}

console.log(fund(4, 3));

//Arrow Functions

var ken = (k, f) => {
    return mult = k * f;
}

console.log(ken(4, 5));